{
  "name": "lndhub-proxy",
  "version": "1.0.1",
  "description": "Minimal, allow-by-construction LNDhub API proxy for a single LND node. No database, no accounts, no on-chain endpoints.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/smoke.mjs"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  }
}
